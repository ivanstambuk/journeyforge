# Feature 001 – Tasks

- [ ] T-001-01: Create model types in `journeyforge-model` (records/enums for Workflow, State, HttpCall).
- [ ] T-001-02: Parser with schema validation in `journeyforge-parser` (YAML + JSON).
- [ ] T-001-03: HTTP connector (interface + Java 25 HttpClient impl) in `journeyforge-connectors-http`.
- [ ] T-001-04: Runtime executor in `journeyforge-runtime-core` (sequential, synchronous).
- [ ] T-001-05: Temporary runner in `app/` to execute a workflow file and print JSON outcome.
- [ ] T-001-06: Unit tests for parser (valid/invalid specs) and runtime (success/failure paths) using stubbed HTTP.
- [ ] T-001-07: Add example workflow and update docs references.

Notes:
- Keep tasks ≤90 minutes each; prefer fine‑grained commits.
- Add or update open questions as needed and reference their IDs in commits.

