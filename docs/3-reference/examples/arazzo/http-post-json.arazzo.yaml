arazzo: 1.0.1
info:
  title: JourneyForge – http-post-json workflows
  version: 0.1.0
  summary: Workflows for HTTP POST with JSON body and 201 checks.
sourceDescriptions:
  - name: httpPostJson
    type: openapi
    url: ../oas/http-post-json.openapi.yaml
workflows:
  http-post-json-happy-path:
    summary: Start an http-post-json journey and retrieve the final outcome.
    sourceDescriptions:
      - httpPostJson
    inputs:
      startRequest:
        description: Initial payload for the http-post-json journey.
        schema:
          type: object
          required:
            - inputId
          properties:
            inputId:
              type: string
          additionalProperties: true
    steps:
      - name: startJourney
        description: Start a new http-post-json journey instance.
        call:
          source: httpPostJson
          operationId: httpPostJson_start
        parameters:
          requestBody: $inputs.startRequest
        outputs:
          journeyId: $response.body.journeyId
      - name: getResult
        description: Retrieve the final outcome after the POST.
        call:
          source: httpPostJson
          operationId: httpPostJson_getResult
        parameters:
          path:
            journeyId: $steps.startJourney.outputs.journeyId

