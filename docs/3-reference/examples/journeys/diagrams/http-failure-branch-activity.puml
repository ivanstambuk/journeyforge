@startuml
!include journey-diagram-theme.puml
title http-failure-branch – client activity flow

start

:Client sends\nPOST /api/v1/journeys/http-failure-branch/start;
:Client polls\nGET /api/v1/journeys/{journeyId}/result\nuntil phase != "Running";

if (phase == "SUCCEEDED"?) then (yes)
  :Client processes successful\nJourneyOutcome.output;
else (no)
  :Client handles failure via\nJourneyOutcome.error;
endif

stop

@enduml
