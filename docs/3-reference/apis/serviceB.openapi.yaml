openapi: 3.1.0
info:
  title: ServiceB API
  version: 1.0.0
  description: ServiceB API
  contact:
    name: JourneyForge
servers:
  - url: https://api.example.com
paths:
  /serviceB/{id}:
    get:
      operationId: getServiceBById
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
        "500":
          description: Error
      description: serviceB GET /serviceB/{id}
      tags:
        - serviceB
tags:
  - name: serviceB
