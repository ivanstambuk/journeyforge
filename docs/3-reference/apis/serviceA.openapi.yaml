openapi: 3.1.0
info: { title: ServiceA API, version: 1.0.0 }
servers: [ { url: https://api.example.com } ]
paths:
  /serviceA/{id}:
    get:
      operationId: getServiceAById
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200': { description: OK, content: { application/json: { schema: { type: object } } } }
        '500': { description: Error }
